import { IntervalTimePipe } from './interval-time.pipe';

describe('IntervalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new IntervalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
